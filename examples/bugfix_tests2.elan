@erelang

action sys_cmd_test {
    import "builtin/process" as proc;
    string out = proc.shell("echo cmd_hello");
    print "cmd_out={out}";
    string out2 = proc.shell("echo {out}");
    print "cmd_out2_len={string.len(out2)}";
}

action main {
    sys_cmd_test();
}

run main;
