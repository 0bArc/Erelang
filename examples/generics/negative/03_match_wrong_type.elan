@strict

enum Option<T> {
    Some(T),
    None
}

public action main() {
    Option<int> o = Option<int>.Some(1);
    match (o) {
        case Some(x): {
            string y = x;
            print y;
        }
        case None: {
            print "empty";
        }
    }
}

run main;
