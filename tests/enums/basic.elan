@strict

enum Color {
    Red,
    Green,
    Blue
}

public action main() {
    Color c = Color.Red;
    match (c) {
        case Red: { print "red"; }
        case Green: { print "green"; }
        case Blue: { print "blue"; }
    }
    print "enums-ok";
}

run main;
