public action helper(x: int): int {
  return x + 1;
}

public action main() {
  int sum = 0;
  for (int i = 0; i < 5; i = i + 1) {
    sum = sum + helper(i);
  }

  try {
    print "sum:{sum}";
  } catch (err) {
    print "caught:{err}";
  }
}

run main;