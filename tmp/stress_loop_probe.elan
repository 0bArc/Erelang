public action inc(x: int): int { return x + 1; }
public action add(a: int, b: int): int { return a + b; }

public action main() {
  int i = 0;
  int acc = 0;
  while (i < 2000) {
    int t = inc(i);
    acc = add(acc, t);
    i = i + 1;
  }
  print "acc:{acc}";
}

run main;